# Runtime dependencies for serverless / container deployment.
#
# Local development uses pyproject.toml via `uv sync --extra dev`; this file
# exists because Vercel's Python runtime installs from requirements.txt.
#
# Deliberately excluded to keep the serverless bundle small: asyncpg and
# pgvector (imported lazily, only needed when DATABASE_URL is set) and voyageai
# (only needed for EMBEDDING_PROVIDER=voyage). Add them back if you use those.
fastapi>=0.111.0
pydantic>=2.7.0
httpx>=0.27.0
numpy>=1.26.0
pyyaml>=6.0.1
python-dotenv>=1.0.0
openai>=1.30.0
anthropic>=0.28.0
